{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "build:lib": "pnpm build --filter lib"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "eslint-plugin-jsx-a11y",
        "eslint-plugin-react-hooks",
        "eslint-plugin-react",
        "rollup"
      ]
    }
  },
  "packageManager": "pnpm@6.32.2",
  "devDependencies": {
    "eslint": "^8.10.0",
    "prettier": "^2.5.1",
    "turbo": "^1.1.4"
  }
}
